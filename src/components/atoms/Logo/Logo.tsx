import Logoimg from '../../../assets/images/logo.png';

const Logo = () => {
  return (
    <div>
      <img style={{ cursor: 'pointer' }} src={Logoimg} alt="logo" />
    </div>
  );
};

export default Logo;
